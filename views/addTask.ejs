<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" type="text/css" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/taskScreen.css" media="screen">
    <title>Añadir Tarea</title>
</head>
<body>
    <!-- TODO: crear formulario para añadir tareas --> 
    <header>
        <img class="logo" src="/img/logo.png" alt="Gestor de Tareas"> 
        <div class="user">
            <img class="user-icon" src="/img/noUser.png" alt="">
            <div class="user-info">
                <p>usuario@ucm.es</p>
                <button class="btn btn-outline-light" type="logout">Desconectar</button>
            </div>
        </div>
    </header>
    <main">
        <form class="add-task-form" action="/tasks/addTask" method="POST">
            <% if (errorMessage) {%> 
                <div class="alert alert-danger error" role="alert"><%= errorMessage %></div>
            <% } %> 
            <label for="task">Añadir Tarea</label>
            <input class="new-task-text" type="text" name="task" id="task" placeholder="Nombre de la tarea @tag1 @tag2 ...">
            <div class="form-buttons">
                <a class="btn btn-danger" href="/tasks">Cancelar</a>
                <button class="btn btn-success" type="submit">Añadir</button>
            </div>
        </form>
    </main>
</body>
</html>